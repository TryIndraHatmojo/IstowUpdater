import QtQuick

/*!
 * PatchDbTableAccordion — Stub component (tidak digunakan).
 * Fitur Patch DB Data sekarang menggunakan layout sidebar + panel.
 */
Item {
    id: root
    property string tableName: ""
    property var patchModel: null
}
